import 'package:flutter/material.dart';

/// App color scheme based on JSON specification
class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF0D2652); // Dark Navy Blue from images
  static const Color primaryLight = Color(0xFFD4E3FF); // Light Blue from images
  static const Color accent = Color(0xFF4F83FF);
  
  // Background colors
  static const Color background = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFF8FAFC);
  static const Color lightGray = Color(0xFFF1F5F9);

  // Text colors
  static const Color textPrimary = Color(0xFF0D2652);
  static const Color textSecondary = Color(0xFF64748B);

  // Action colors
  static const Color border = Color(0xFFCBD5E1);
  static const Color success = Color(0xFF22C55E);
  static const Color error = Color(0xFFEF4444);

  // Dark theme colors (Soft Dark/Slate)
  static const Color darkBackground = Color(0xFF1E293B);
  static const Color darkSurface = Color(0xFF334155);
  static const Color darkTextPrimary = Color(0xFFE2E8F0);
  static const Color darkTextSecondary = Color(0xFF94A3B8);
}

